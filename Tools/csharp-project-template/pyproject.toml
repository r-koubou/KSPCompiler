[project]
name = "csharp-project-template"
version = "0.1.0"
description = "A template project for generating csproj files"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "Jinja2 >=3.*",
    "ruamel-yaml>=0.19.*",
]
