name: Build KSP Compiler (develop)

on:
  push:
    branches:
      - develop
      - feature/**
      - fix/**

  workflow_dispatch:

jobs:
  build-compiler-develop:
    uses: ./.github/workflows/build_compiler.yml
    secrets: inherit
    with:
      channel: develop
