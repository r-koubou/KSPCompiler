name: Build Language Server (develop)

on:
  push:
    branches:
      - develop
    paths-ignore:
      - "**.md"

  workflow_dispatch:

jobs:
  build-language-server-develop:
    uses: ./.github/workflows/build_language_server.yml
    secrets: inherit
