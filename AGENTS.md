## 目的

Native Instruments KONTAKT が提供するスクリプト言語 KSP (Kontakt Script Processor) のコンパイラ・Language Server を開発するためプロジェクト。

## ドキュメント

.agents/ ディレクトリに仕様やコンテキストをまとめる。


## プロジェクトを通じたルールに関する注意点

コードの実装・設計を行う前に、該当するルールを必ず読むこと。

- `.agents/rules/???????.md`  - コーディング規約


## プランニングとコンテキスト管理

### 1. プランの永続化

- `Plan Mode`（または計画フェーズ）でタスク分解や実装計画を策定した際は、実行に移る前、またはセッションを終了する前に、必ずその内容を `.agents/plans/` 以下にマークダウン形式で書き出し、または同期（アップデート）すること。
    - ファイル名は `YYYY-MMDD-＜行う予定の名称（32文字程度で収まる、英語＋ケバブケース）＞.md` の形式にすること。
        - 行う予定の名称の判断が困難な場合は人間に確認すること。
- プランには「全体のゴール」「タスクリスト（チェックボックス `- [ ]` 形式）」「懸念点・設計方針」を含めること。
- タスクが完了するごとに、チェックボックスを `[x]` に更新すること。

### 2. 仕様書

- 前述のプランモードを経て実行に移る際、新規に仕様書を作成する場合は `.agents/specs/` 以下にマークダウン形式で書き出すこと。
- ファイル名は `＜行う予定の名称（32文字程度で収まる、英語＋ケバブケース）＞.md` の形式にすること。
- 実行直後の動作確認の過程で仕様をアップデートした場合は合わせてファイル内容を更新すること。
- 改修を目的としたセッションにおいても、仕様書の更新が必要な場合は同様にファイル内容を更新すること。

### 3. 実行の結果の永続化

- 実行フェーズで行ったこと、実行フェーズ途中で協議した内容など状態の記録は `.agents/contexts/README.md` を参照し、記録すること。

### 4. ディスカッションの永続化

プランや実行に満たない、人間側からの質問やディスカッションレベルの議論や相談から開始されるセッションを指す。

#### 永続化のタイミング

- 人間側が明示的に「このディスカッションを記録してほしい」という旨の指示があった時点


#### ファイルパス

- ディスカッション内容を整理し `.agents/discussions/` 以下にマークダウン形式で書き出すこと。
    - ファイル名は `YYYY-MMDD-＜セッション名（32文字程度で収まる、英語＋ケバブケース）＞.md` の形式にすること。
        - セッション名の判断が困難な場合は人間に確認すること。

#### 特記事項

- ディスカッション過程でファイルの作成、既存ファイルの更新が必要な場合は人間に確認を行うこと。


## ディレクトリ構成


本プロジェクトのプログラムコードは Compiler ディレクトリに配備され、その他のディレクトリは、コンパイラの実行に必要なデータやツールを配備する。

```
├── .agents             AIエージェントp向けのドキュメントや仕様書を配備
├── Compiler            コンパイラ・Language Server の C# ソリューション
├── Data                コンパイラが起動時に読み込むビルトインのシンボルデータを配備
├── Grammar             KSP の文法定義を配備 (ANTLRを使用)
└── Tools               小規模なメンテナンスツール各種
```
