on init
    declare ui_table %tbl[10](1,2,3)
    declare @i
    declare $j
    declare $k
    message( $k )
    $j := .not. $k
    $k := 1
    @i := 234
    $x := 0 {error: variable not declared}
end on

on note
    $d := 0 {error: variable not declared}
end on
